#include<stdio.h>
int main()
{
	int result = 1;
	int Limak;
	int Bob;
	int i;
	scanf("%d %d", &Limak, &Bob);
	for(i = 0; i < 100; i++)
	{
		if(Limak > Bob)
		{
			printf("%d",i);
			return 0;
		}
		Limak = Limak*3;
		Bob = Bob*2;
	}
	return 0;
}
